export enum ROUTE_PATH {
    HOME = "/",
    HISTORY = "/history",
    MODELS = "/models",
    LOCAL_MODELS = "/local-models",
    ENHANCER = "/enhancer",
    VOICE_SETTINGS = "/voice-settings",
    SETTINGS = "/settings",
}
