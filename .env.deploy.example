# Deployment Configuration for Local macOS Builds
# Copy this file to .env.deploy and fill in your values

# SSH Configuration
SSH_HOST="your-server.com"
SSH_USER="deploy"
SSH_PORT="22"
SSH_PRIVATE_KEY_PATH="$HOME/.ssh/id_rsa"

# Deployment Path
DEPLOY_PATH="/var/www/downloads"

# Base URL (for auto-update manifest)
BASE_URL="https://downloads.example.com"

# Tauri signing key (required for auto-update to work)
# Generate with: pnpm tauri signer generate
TAURI_SIGNING_PRIVATE_KEY="your-private-key-content"
TAURI_SIGNING_PRIVATE_KEY_PASSWORD="your-password"
