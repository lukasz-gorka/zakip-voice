// Tailwind CSS v4 uses CSS-first configuration. This file is intentionally minimal.
/** @type {import('tailwindcss').Config} */
module.exports = {
    // v4 auto-detects content and uses CSS variables; keep this file for any future JS-based extensions.
};
